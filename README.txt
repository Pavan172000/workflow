BIS Workflow Management – Update Package (v2.3f)

Tighter compact layout (per your “yes”):
- Header: `h1 = 1.25rem`, header padding `8px 10px`, nav buttons `5px 8px`.
- Filters: control padding `5px 7px`.
- Table: cell padding `5px`, actions buttons `4px 7px`, body font-size `9.5px`.
- Taller data viewport: `max-height: calc(100vh - 160px)` on Dashboard/Completed.

All prior features retained: BYYMMNNNN IDs, NRA freeze (Workflow Name + Received + Deadline), actions no-wrap, modal “View”, XLSX/CSV export.
